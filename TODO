HACER PARA Que autodetecte cuales ups hay en la maquina local y permita leer la informacion del ups de /etc/ups/ups.conf by default, que seleccione con una lista DONE
ver si vale la pena migrar a LIST UPS en lugar de leer el archivo conf

session management support (for multiple windows), open in new window option (como le paso el pw al nuevo app?)

remember recently successfully monitored upses show them in a list box

command-line ups specification: DONE
lacks --help and GTK command line processing

event log window?

mover headers a GtkHeaders instead of GtkLabels?

gnome support?  #ifdef'd ?

show the true Description field (ACP BLABLA) instead of the ups name (apc) if available


